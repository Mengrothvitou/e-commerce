import Head from 'next/head'
export default function Header(){
    return(
        <Head>
            <title>Eunoir || Online shops</title>
            <meta
                name="viewport"
                content="minimum-scale=1, initialize-scale=1, width=device-width"></meta>
        </Head>
        )
}